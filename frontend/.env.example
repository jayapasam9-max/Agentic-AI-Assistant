# In dev, leave VITE_API_BASE_URL empty and let Vite proxy /api to VITE_BACKEND_URL.
# In production (Vercel), set VITE_API_BASE_URL to the Render backend origin.
VITE_API_BASE_URL=
VITE_BACKEND_URL=http://localhost:8080
